from schema import Schema, And, Use, Optional, Regex

schema = Schema({
    "operations": [
        {
            "type": "hexToDec",
            "def": []
        }
    ]
})

schema_def = Schema([])
